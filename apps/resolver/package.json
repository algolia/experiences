{
  "name": "@experiences/resolver",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "portless resolver sh -c 'wrangler dev --port $PORT'",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "algoliasearch": "^5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.0",
    "@cloudflare/workers-types": "^4",
    "@experiences/typescript-config": "*",
    "typescript": "5.9.2",
    "vitest": "3.0.5",
    "wrangler": "^4"
  }
}
